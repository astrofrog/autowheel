This directory includes a yml config file that is used to run tests. We use a
pure-Python package which has universal wheel builds so that the tests will
always think they need to build platform-specific wheels.
